# 🚀 Быстрый старт: Деплой на VPS

## Вариант 1: Автоматическая установка

### На вашем ПК (Windows PowerShell):

```powershell
# 1. Загрузите все файлы на сервер
scp -r C:\bot root@ВАШ_IP:/opt/

# 2. Подключитесь к серверу
ssh root@ВАШ_IP
```

### На сервере:

```bash
cd /opt/bot

# Выполните автоматическую установку
chmod +x install.sh
./install.sh
```

---

## Вариант 2: Ручная установка (пошагово)

### 1. Подготовка сервера

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка необходимого ПО
apt install python3 python3-pip python3-venv git nano -y
```

### 2. Создание директории и загрузка файлов

```bash
# Создайте директорию
mkdir -p /opt/bot
cd /opt/bot
```

**На вашем ПК** загрузите файлы:
```powershell
# Из PowerShell на Windows
scp C:\bot\bot_polya.py root@ВАШ_IP:/opt/bot/
scp C:\bot\requirements.txt root@ВАШ_IP:/opt/bot/
scp C:\bot\.env root@ВАШ_IP:/opt/bot/
scp C:\bot\reports.db root@ВАШ_IP:/opt/bot/
scp C:\bot\token.json root@ВАШ_IP:/opt/bot/
scp C:\bot\oauth_client.json root@ВАШ_IP:/opt/bot/
scp C:\bot\service_account.json root@ВАШ_IP:/opt/bot/
scp C:\bot\bot_polya.service root@ВАШ_IP:/opt/bot/
```

### 3. Настройка Python окружения

```bash
cd /opt/bot

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate

# Установите зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. Проверка запуска

```bash
# Попробуйте запустить бота вручную
python bot_polya.py
```

Нажмите `Ctrl+C` чтобы остановить, если всё работает.

### 5. Настройка systemd service

```bash
# Скопируйте файл службы
cp bot_polya.service /etc/systemd/system/

# Перезагрузите systemd
systemctl daemon-reload

# Включите автозапуск
systemctl enable bot_polya

# Запустите службу
systemctl start bot_polya
```

### 6. Проверка статуса

```bash
# Статус службы
systemctl status bot_polya

# Просмотр логов в реальном времени
journalctl -u bot_polya -f

# Последние 50 строк логов
journalctl -u bot_polya -n 50
```

---

## 🎯 Основные команды управления

```bash
# Запуск
systemctl start bot_polya

# Остановка
systemctl stop bot_polya

# Перезапуск
systemctl restart bot_polya

# Статус
systemctl status bot_polya

# Просмотр логов
journalctl -u bot_polya -f
```

---

## 🔄 Обновление бота

```bash
cd /opt/bot

# Загрузите обновленные файлы через scp или git pull

# Перезапустите службу
systemctl restart bot_polya

# Проверьте статус
systemctl status bot_polya
```

---

## 🐛 Решение проблем

### Бот не запускается

```bash
# Посмотрите логи для деталей
journalctl -u bot_polya -n 100 --no-pager

# Проверьте .env файл
cat /opt/bot/.env

# Попробуйте запустить вручную
cd /opt/bot
source venv/bin/activate
python bot_polya.py
```

### Ошибка "ModuleNotFoundError"

```bash
# Переустановите зависимости
cd /opt/bot
source venv/bin/activate
pip install --upgrade -r requirements.txt
systemctl restart bot_polya
```

### Проблемы с правами доступа

```bash
# Дайте права на файлы
chmod -R 755 /opt/bot
chown -R root:root /opt/bot
```

---

## 📊 Мониторинг

### Просмотр использования ресурсов

```bash
# Процессы Python
ps aux | grep python

# Использование памяти
free -h

# Использование диска
df -h
```

### Автоматический мониторинг и перезапуск

Systemd автоматически перезапустит бота при сбое благодаря параметру `Restart=always`.

---

## ✅ Готово!

Теперь ваш бот работает 24/7 на сервере независимо от вашего ПК! 🎉

Для управления используйте команды systemctl через SSH.



