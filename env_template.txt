# Токен бота Telegram (получите у @BotFather)
BOT_TOKEN=your_bot_token_here

# Часовой пояс (по умолчанию Москва)
TZ=Europe/Moscow

# ID администраторов (через запятую, без пробелов)
ADMIN_IDS=

# Username администраторов (через запятую, без @)
ADMIN_USERNAMES=

# Доп. роли (через запятую): IT и TIM
# IT_IDS могут использовать тестовые команды и видеть расширенное меню
IT_IDS=
# TIM_IDS — специальные пользователи (TIM)
TIM_IDS=

# Пользователи (user_id через запятую), чьи отчёты НЕ публикуем в общий чат/топик статистики,
# но продолжаем сохранять и выгружать в Google Sheets.
HIDE_IDS=

# ===== Google Sheets настройки =====

# OAuth клиент для Google API (путь к файлу oauth_client.json)
OAUTH_CLIENT_JSON=oauth_client.json

# Путь к файлу с токеном (создастся автоматически после первой авторизации)
TOKEN_JSON_PATH=token.json

# ID папки на Google Drive, куда будут сохраняться таблицы
# Получить ID папки: откройте папку на Drive, скопируйте ID из URL
# https://drive.google.com/drive/folders/[ВОТ_ЭТО_ID]
# Можно указать и полный URL папки — бот сам вытащит ID.
DRIVE_FOLDER_ID=

# Префикс для названия таблиц (например: "ОТД" или "Бригадиры")
EXPORT_PREFIX=ОТД

# Отдельная папка на Google Drive для выгрузки бригадиров
# Можно указать и ID, и URL папки.
BRIGADIER_FOLDER_ID=

# Префикс названия таблиц бригадиров
BRIG_EXPORT_PREFIX=Бригадиры

# Включить автоматический экспорт (true/false)
AUTO_EXPORT_ENABLED=false

# Расписание автоматического экспорта в формате cron (5 чисел через пробел)
# Формат: минута час день месяц день_недели
# Примеры:
# 0 9 * * 1    - каждый понедельник в 9:00
# 0 0 * * 0    - каждое воскресенье в 00:00
# 0 18 * * 5   - каждую пятницу в 18:00
AUTO_EXPORT_CRON=0 9 * * 1

# ===== Моментальная синхронизация в Google Sheets =====
# Если true — бот будет автоматически выгружать изменения (добавление/изменение/удаление)
# почти сразу после действий пользователя.
AUTO_EXPORT_ON_CHANGE=false

# Debounce в секундах: если подряд много изменений — объединяем в одну выгрузку
AUTO_EXPORT_ON_CHANGE_DEBOUNCE=3

# ===== Read-only чат (только отчёты) =====
# В этом чате бот НЕ реагирует на команды/текст, а сообщения обычных пользователей удаляются.
# Оставьте пустым, если не нужно.
READONLY_CHAT_ID=