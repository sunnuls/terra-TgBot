# Шаблон переменных для backend (Terra App API).
# Скопируйте в backend/.env и подставьте значения.
# Полный список полей: backend/app/core/config.py

SECRET_KEY=change-me-long-random-string

DB_HOST=localhost
DB_PORT=5432
DB_USER=terra
DB_PASS=terra
DB_NAME=terra_app

REDIS_URL=redis://localhost:6379/0

DEBUG=false

# Опционально
SENTRY_DSN=

# Google (экспорт и т.д., если используете)
GOOGLE_SERVICE_ACCOUNT_FILE=service_account.json
DRIVE_FOLDER_ID=
BRIGADIER_FOLDER_ID=
